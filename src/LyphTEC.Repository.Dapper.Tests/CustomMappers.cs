using DapperExtensions.Mapper;

namespace LyphTEC.Repository.Dapper.Tests
{
    /*
    // NOTE: This gets loaded by DapperExtensions automatically
    class CustomerMapper : ClassMapper<Model.Customer>
    {
        public CustomerMapper()
        {
            Map(x => x.Id).Key(KeyType.Identity);
            
            // hidden by inherited class
            //Map(x => x.Address).Ignore();
            //Map(x => x.SupportRepId).Ignore();

            AutoMap();
        }
    }
     */
}
